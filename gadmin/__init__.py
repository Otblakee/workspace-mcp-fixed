# Google Admin SDK (Directory + Reports) — READ-ONLY ONLY.
#
# This module is strictly read-only by design. No write capability under any
# circumstances. All admin writes (user provisioning, group changes, role
# assignments, OAuth grant revocation, device wipes) stay on GAM CLI / the
# Admin Console. See gadmin/admin_tools.py for the read-only invariant.
